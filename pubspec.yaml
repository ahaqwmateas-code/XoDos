name: xodos
description: A powerful remote workstation and gaming emulator.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gamepads: ^0.1.10+1
  # Add other dependencies your project already uses here, e.g.:
  # http: ^1.0.0
  # path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
